"""VignOCR — enterprise OCR for Algerian pharmaceutical vignettes.

Two-stage pipeline (detection -> recognition) + deterministic parsing,
nomenclature correction, and reimbursability classification. Every module
is config-driven; the field schema lives once in ``configs/classes.yaml``.
"""

__version__ = "0.1.0"
